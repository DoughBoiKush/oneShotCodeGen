import { Container, Typography, Button } from '@mui/material';import { useNavigate } from 'react-router-dom';import { Header } from '../components/layout/Header';export const ErrorPage = ({ code, message }) => {const navigate = useNavigate();return (<><Header title="FriendFlix" /><Container sx={{ textAlign: 'center', mt: 4 }}><Typography variant="h4" gutterBottom>{code} - {message}</Typography><Button variant="contained" onClick={() => navigate('/')}>Go Home</Button></Container></>);};
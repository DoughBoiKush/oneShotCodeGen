import React from 'react';import {Box,Typography,Button} from '@mui/material';export const ErrorBoundary=({error,resetErrorBoundary})=>(<Box sx={{p:3,textAlign:'center'}}><Typography variant='h5' color='error'>Something went wrong!</Typography><Typography sx={{mt:2}}>{error.message}</Typography><Button onClick={resetErrorBoundary} variant='contained' sx={{mt:2}}>Try again</Button></Box>);
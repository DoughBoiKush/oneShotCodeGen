import { useQuery, useMutation, useQueryClient } from 'react-query';import { getLeaveRequests, createLeaveRequest, updateLeaveStatus } from '../services/api';export const useLeaveRequests = () => {const { data: leaves = [], isLoading, error } = useQuery('leaves', getLeaveRequests);const queryClient = useQueryClient();const createMutation = useMutation(createLeaveRequest, {onSuccess: () => queryClient.invalidateQueries('leaves')});const updateMutation = useMutation(({id, status}) => updateLeaveStatus(id, status), {onSuccess: () => queryClient.invalidateQueries('leaves')});return { leaves, isLoading, error, createLeave: createMutation.mutate, updateLeaveStatus: updateMutation.mutate };};
import { Box, Typography, Button } from '@mui/material';import { useNavigate } from 'react-router-dom';export const ErrorPage = () => {const navigate = useNavigate();return (<Box sx={{display: 'flex',flexDirection: 'column',alignItems: 'center',justifyContent: 'center',minHeight: '100vh'}}><Typography variant='h1'>500</Typography><Typography variant='h5'>Something went wrong</Typography><Button variant='contained' onClick={() => navigate('/')} sx={{ mt: 2 }}>Go Home</Button></Box>);};